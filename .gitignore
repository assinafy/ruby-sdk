*.gem
# Gemfile.lock is intentionally not committed: this is a published gem, and the
# convention is to resolve dependencies fresh against each supported Ruby in CI
# (Bundler's own guidance for gems). Applications consuming the gem keep their own lock.
Gemfile.lock
.DS_Store
.env
.env.local
coverage/
pkg/
vendor/bundle
.bundle/

# Agent tooling, playbooks, and audit notes: local working files, never committed
# and never shipped in the gem.
.claude/
.codex/
.cursor/
.aider*
AGENTS.md
AGENT.md
CLAUDE.md
CODEX.md
GEMINI.md
.mcp.json
AUDIT*.md
audit/
*-audit.md
*_audit.md
